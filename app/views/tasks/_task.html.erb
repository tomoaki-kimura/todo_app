<tr>
  <td><%= task.title %></td>
  <td><%= l task.start_time, format: :long %></td>
  <td><%= l task.end_time, format: :long if task.end_time.present? %></td>
  <td><%= link_to task.status_i18n, task_path(task), class: "btn #{colord_button(task)}" %></td>
</tr>


